# =============================================================================
# .env.example — Variables d'environnement de l'API Mock MCO
#
# Ce fichier est un MODÈLE documenté. Il est versionné dans git.
# Ne jamais mettre de vraies valeurs secrètes dans ce fichier.
#
# Pour utiliser :
#   cp .env.example .env
#   # Éditer .env selon vos besoins
#   # .env est ignoré par git (voir .gitignore)
#
# Ces variables sont lues par app/config.py via pydantic-settings.
# Elles peuvent aussi être passées directement en ligne de commande :
#   PORT=9000 uvicorn app.main:app --reload
# =============================================================================


# =============================================================================
# Paramètres du serveur
# =============================================================================

# Port d'écoute du serveur.
# Utilisé par uvicorn au démarrage et documenté dans docker-compose.yml.
# Valeur par défaut : 8000
PORT=8000

# Environnement d'exécution.
# Valeurs reconnues : development, production, test
# Valeur par défaut : development
ENVIRONMENT=development


# =============================================================================
# Génération de données mock
# =============================================================================

# Seed du générateur aléatoire.
# Laisser vide (commenté) pour des données différentes à chaque appel.
# Fixer un entier pour des données toujours identiques (démos, tests).
# Exemples :
#   RANDOM_SEED=42    → données déterministes (toujours les mêmes)
#   #RANDOM_SEED=     → données aléatoires (comportement par défaut)
#RANDOM_SEED=42


# =============================================================================
# Configuration CORS
#
# CORS (Cross-Origin Resource Sharing) contrôle quels domaines peuvent
# appeler cette API depuis un navigateur.
#
# En développement : "*" autorise tout (pratique, mais à ne pas utiliser
# en production si l'API expose des données sensibles).
#
# En production : spécifier les origines exactes séparées par des virgules.
# Exemples :
#   CORS_ORIGINS=*
#   CORS_ORIGINS=http://localhost:3838
#   CORS_ORIGINS=http://localhost:3838,https://monapp.example.com
# =============================================================================

CORS_ORIGINS=*
